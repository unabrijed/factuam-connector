{
  "name": "factuam",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*",
    "workers/*"
  ],
  "engines": {
    "node": ">=20",
    "yarn": ">=1.22.0"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:web": "turbo run dev --filter=@factuam/web",
    "dev:api": "turbo run dev --filter=@factuam/api",
    "dev:worker": "turbo run dev --filter=@factuam/ml-runner",
    "web": "yarn workspace @factuam/web dev",
    "api": "yarn workspace @factuam/api dev",
    "worker": "yarn workspace @factuam/ml-runner dev",
    "dev:axl": "node scripts/dev-axl.mjs",
    "local:axl:keys": "node scripts/ensure-axl-local-keys.mjs",
    "local:axl": "node scripts/local-axl-stack.mjs",
    "local:axl:nodes": "node scripts/local-axl-stack.mjs --nodes-only",
    "local:axl:single": "node scripts/local-axl-single.mjs",
    "local:axl:single:nodes": "node scripts/local-axl-single.mjs --nodes-only",
    "check:axl": "node scripts/check-axl-health.mjs",
    "build": "turbo run build",
    "build:web": "yarn workspace @factuam/web build",
    "build:api": "yarn workspace @factuam/api build",
    "typecheck": "turbo run typecheck",
    "typecheck:web": "tsc -p apps/web/tsconfig.json --noEmit",
    "typecheck:api": "yarn workspace @factuam/api typecheck",
    "test": "turbo run test",
    "check:web": "tsc -p apps/web/tsconfig.json --noEmit",
    "check": "yarn typecheck && yarn test",
    "db:generate": "turbo run db:generate --filter=@factuam/api",
    "db:migrate": "turbo run db:migrate --filter=@factuam/api",
    "clean": "find . \\( -name .next -o -name dist -o -name .turbo -o -name '*.tsbuildinfo' \\) -prune -exec rm -rf {} +"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "turbo": "^2.0.14",
    "typescript": "^5.6.3"
  }
}
